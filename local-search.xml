<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Linux部署后端服务器</title>
    <link href="/Linux%E9%83%A8%E7%BD%B2%E5%90%8E%E7%AB%AF%E6%9C%8D%E5%8A%A1%E5%99%A8/"/>
    <url>/Linux%E9%83%A8%E7%BD%B2%E5%90%8E%E7%AB%AF%E6%9C%8D%E5%8A%A1%E5%99%A8/</url>
    
    <content type="html"><![CDATA[<p>最近在做微信小程序的项目，我负责后端的部分。由于项目组人数比较少，所以采用前后端分离的开发模式开发整个项目。在开发过程中难免需要为前端人员提供接口与数据，所以项目过程中将测试程序部署到服务器，让前端通过域名访问的形式来访问接口、接发数据是很有必要的。我在部署的过程中遇到了不少困难，但是在通过查阅资料后找到了最简洁的部署方式，便记录下来帮助有需要的读者。</p><p>由于微信小程序不需要部署前端项目，只需要将后端部署在服务器上即可，故以下内容仅涉及如何部署一个<strong>后端接口服务器</strong>。</p><h3 id="一jar-包">一、Jar 包</h3><p>我使用 <strong>SpringBoot</strong> 框架开发 Java 后端程序。SpringBoot框架基于 <strong>Maven</strong> 进行项目管理，并且自带 Tomcat服务器，通过 Maven 的 【package】功能可以很容易将项目打包成 Jar的形式。</p><h4 id="vscode">1. 1 VSCode</h4><p>如果你使用 <strong>VSCode</strong> 开发代码的话，【Maven forJava】插件提供了图形化的管理界面。开发完成后，依次点击左下角的【MAVEN】-&gt;【Your_Project_name】-&gt;【Lifecycle】-&gt;【package】，即可将程序打包成Jar 包。</p><h4 id="命令行">1. 2 命令行</h4><p>如果你使用其他的开发工具开发，也可以使用<strong>命令行</strong>的格式对项目打包。进入到项目路径下，打开终端输入：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">mvn package -f &quot;d:\path\to\your\project\pom.xml&quot;<br></code></pre></td></tr></table></figure><p>通过上面两种方式打包完成后，Jar 包会被存放在<code>"d:\path\to\your\project\target"</code> 路径下，命名为<code>Your_Project_name.jar</code>。</p><h3 id="二nginx">二、Nginx</h3><p>由于生成的 Jar 自带 <strong>Tomcat</strong>服务器，启动之后默认端口为 localhost:8080，所以我们可以不用单独设置Tomcat 服务器。</p><p>虽然说我们不部署前端程序，Tomcat服务器也可以直接被访问，但是我们依然可以使用 <strong>Nginx</strong> 作为Tomcat 的代理服务器。</p><p>Nginx 用来作为 Tomcat 代理服务器有很多优点：</p><ul><li>（如果你要部署前端项目）Nginx可以非常高效地处理<strong>静态文件</strong>，可以减轻 Tomcat服务器的压力。</li><li>Nginx 可以为 Tomcat 提供<strong>反向代理</strong>，隐藏 Tomcat的真实地址，提升了安全性。</li><li>Nginx 可以做<strong>负载均衡</strong>，当有多个 Tomcat服务器时可以均匀分配业务，提升了性能和拓展性。</li></ul><p>安装好 Nginx 后，需要配置 Nginx 实现后端代理功能。在 Linux终端中输入命令：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo vim /etc/nginx/sites-enabled/default<br></code></pre></td></tr></table></figure><p>具体文件路径可以自行查找。在打开的文件夹中找到并修改为以下字段：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-comment"># Add index.php to the list if you are using PHP</span><br><span class="hljs-attribute">index</span> index.html index.htm index.nginx-debian.html;<br><br><span class="hljs-attribute">server_name</span> Your_Domain;<br><br><span class="hljs-section">location</span> / &#123;<br>        <span class="hljs-comment"># First attempt to serve request as file, then</span><br>        <span class="hljs-comment"># as directory, then fall back to displaying a 404.</span><br>        <span class="hljs-comment"># try_files $uri $uri/ =404;</span><br>        <span class="hljs-attribute">proxy_pass</span> http://localhost:8080;  <span class="hljs-comment"># Tomcat监听的地址</span><br>&#125;<br></code></pre></td></tr></table></figure><p>其中：</p><ul><li><code>server_name</code>：修改为你的域名网址</li><li>注释掉 <code>try_files $uri $uri/ =404;</code></li><li>添加 <code>proxy_pass http://localhost:8080;</code>，用于反向代理Tomcat 服务器</li></ul><p>配置好后，在终端中输入以下指令用于重载配置：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo nginx -t<br>sudo systemctl restart nginx<br></code></pre></td></tr></table></figure><h3 id="三启动-jar-包">三、启动 Jar 包</h3><p>配置好 Nginx 后，只需要操作 Java 启动 Jar 包就行了。默认 Linux安装的版本可能不是适配的版本，请前往 <ahref="https://www.oracle.com/cn/java/technologies/downloads/">Oracle</a>官网下载适配版本。</p><p>你可以在终端中输入 <code>uname -m</code> 查看服务器架构。</p><h4 id="手动启动">3. 1 手动启动</h4><p>将你的文件上传到 Linux 服务器，进入到 Jar包所在的路径下，输入指令：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">nohup java -jar your_project_name.jar &amp;&gt; project.log &amp;<br></code></pre></td></tr></table></figure><p>其中：</p><ul><li><code>nohup</code> 表示程序将以后台运行的方式启动</li><li>程序输出的内容将存放到 <code>project.log</code> 文件夹下</li></ul><p>此时你的服务器应该能正常通过域名访问了。</p><h4 id="自动启动">3. 2 自动启动</h4><p>如果你想你的服务器自动启动，可以通过 Linux 的 <code>systemctl</code>来管理服务。</p><p>首先需要新建一个 Shell 脚本，用于输出日志文件和启动程序：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo vim start_app.sh<br></code></pre></td></tr></table></figure><p>然后在文件中输入：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs shell"><span class="hljs-meta prompt_">#</span><span class="language-bash">!/bin/bash</span><br><span class="hljs-meta prompt_"># </span><span class="language-bash">定义日志文件路径</span><br>LOG_DIR=&quot;/path/to/your/log&quot;<br>CURRENT_LOG=&quot;$LOG_DIR/CURRENT_LOG.log&quot;<br><span class="hljs-meta prompt_"># </span><span class="language-bash">如果日志文件存在，则重命名为程序退出的时间</span><br>if [ -f &quot;$CURRENT_LOG&quot; ]; then<br>    TIMESTAMP=$(date +&quot;%Y%m%d_%H%M%S&quot;)<br>    mv &quot;$CURRENT_LOG&quot; &quot;$LOG_DIR/id_$TIMESTAMP.log&quot;<br>fi<br><span class="hljs-meta prompt_"># </span><span class="language-bash">创建新的日志文件</span><br>touch &quot;$CURRENT_LOG&quot;<br><span class="hljs-meta prompt_"># </span><span class="language-bash">更改日志文件的权限</span><br>chmod 644 &quot;$CURRENT_LOG&quot;<br><span class="hljs-meta prompt_"># </span><span class="language-bash">启动Java应用并将输出重定向到日志文件</span><br>/path/to/your/java -jar your_project_name.jar &gt;&gt; &quot;$CURRENT_LOG&quot; 2&gt;&amp;1<br></code></pre></td></tr></table></figure><p>其中程序如果退出，脚本则会将日志文件保存成【时间.log】的形式，方便对日志文件进行管理。</p><p>保存之后，赋予执行权限：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo chmod +x start_app.sh<br></code></pre></td></tr></table></figure><p>创建好执行脚本后，新建一个服务单元文件：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo vim /etc/systemd/system/your_project_name.service<br></code></pre></td></tr></table></figure><p>然后在文件中输入：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs shell">[Unit]<br>Description=Intelligent Diagnosis Java App<br>After=network.target<br>[Service]<br>User=ubuntu<br>WorkingDirectory=/home/ubuntu/app<br>ExecStart=/path/to/your/shell/start_app.sh<br>SuccessExitStatus=143<br>Restart=always<br>RestartSec=10<br>[Install]<br>WantedBy=multi-user.target<br></code></pre></td></tr></table></figure><p>其中：<code>Description</code>表示启动时显示的描述信息，<code>After</code>表示开启网络连通之后才会启动程序的功能，<code>User</code>表示服务通过哪个用户启动，<code>WorkingDirectory</code>表示服务工作路径，<code>ExecStart</code> 表示执行的指令，<code>SuccessExitStatus</code>表示服务成功退出时的状态码，<code>Restart</code>表示断线自动重连，<code>RestartSec</code>表示重连的间隔时间(s)，<code>WantedBy</code>表示多用户模式下运行时自动启动。</p><p>保存退出后，在终端中输入以下指令是配置生效：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo systemctl daemon-reload<br>sudo systemctl restart your_project_name<br></code></pre></td></tr></table></figure><p>你可以输入以下指令查看程序是否正常运行：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">sudo systemctl status your_project_name<br></code></pre></td></tr></table></figure><h3 id="四总结">四、总结</h3><p>本文仅仅设计一个小型个人项目的服务器该如何部署，如果需要部署更大的项目，或者设置更复杂的服务器集群，特别是本文配置Nginx服务器的部分难免有考虑不周的地方，具体方法请参考其他博客或者期待博主以后的更新^^，不过应该能应付单个服务器的部署了。感谢你看到这里，希望我的解决方法能给你的问题带来一点启示。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Linux</tag>
      
      <tag>后端</tag>
      
      <tag>服务器</tag>
      
      <tag>Nginx</tag>
      
      <tag>Tomcat</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>中国邮递员问题（更新中）</title>
    <link href="/%E4%B8%AD%E5%9B%BD%E9%82%AE%E9%80%92%E5%91%98%E9%97%AE%E9%A2%98/"/>
    <url>/%E4%B8%AD%E5%9B%BD%E9%82%AE%E9%80%92%E5%91%98%E9%97%AE%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<p>今天在写数据结构实验的时候，一不小心选到了图论单元应该最难的实验：<strong>中国邮递员问题</strong>（绝对不是因为题目太短以为很简单所以才选的QAQ）由于题目过于简短，不太容易上手，所以我选择查阅资料学习后再完成。偏偏在各个地方都不太容易找到这个问题的完整求解方法，故博主学习整理之后，将学习的心路历程分享出来，希望能帮助到遇到困难的朋友。</p><span id="more"></span><h2 id="问题描述">1. 问题描述</h2><blockquote><p>在一座城市有一个邮局，邮局的快递员每天在城市不同的区域派送邮件，区域与区域之间有街道相连。快递员希望在派件的时候：</p><ul><li>经过所有区域与区域之间的街道至少一次</li><li>派件结束后回到邮局</li><li>整个路程最短</li></ul><p>请输出符合上述条件的路线。</p></blockquote><p><strong>中国邮递员问题（CPP）</strong>由<strong>管梅谷</strong>教授于1960年提出，将这个问题抽象为图论可以理解为：<u>在一张带非负权连通图上遍历所有的边，最后回到遍历起点，要求总权最小求遍历路径</u>。其中区域可以抽象成顶点，街道可以抽象为边，所管辖的城市可用一个赋权图来表示，其中边的权重表示对应街道的长度。</p><p>对于这个问题已经证明：</p><ul><li>如果只含有双向道（无向图），为P问题。</li><li>如果只含有单向道（有向图），为NPC问题。</li><li>如果既含有双向道，又含有单向道，为NP-Hard问题。</li></ul><blockquote><p>本文在实际生活场景下讨论<strong>无向图</strong>的CPP。</p></blockquote><h2 id="问题分析">2. 问题分析</h2><p>从题目可以很容易看出，既要经过所有的边，又要最短，那最优的情况的就是<u>每条边只走一次</u>，即<strong>一笔画</strong>。围绕这个特点我们可以知道，如果所给图为<strong>欧拉图</strong>，那么存在<strong>欧拉回路</strong>使得每条边都只遍历一次且能回到起点，在这种情况下欧拉回路就是最优环游。</p><p>所以可以将这个问题分成两类：</p><ul><li>所给图为欧拉图，求出欧拉回路。</li><li>所给图为非欧拉图。</li></ul><h3 id="欧拉图">2. 1 欧拉图</h3><p>对于求解欧拉回路有两种解法：Fleury和Hierholzer，不过Hierholzer的效率更高，在这里介绍下<strong>Hierholzer</strong>算法求解欧拉回路。</p><p>Hierholzer算法的核心很简单，就是通过<u>不断删边找到可能的欧拉回路</u>。算法步骤为：</p><ul><li>从当前点开始，遍历关联边对应的相邻点，并将这条关联边删除。</li><li>若当前点没有关联边，则将当前点压入序列栈中。</li></ul><p>我们用一个简单的例子来直观的理解这个算法的思路：</p><figure><img src="/img/中国邮递员问题/graph_1.png" alt="一个简单的示例" /><figcaption aria-hidden="true">一个简单的示例</figcaption></figure><p>在上图中，比较容易知道所有的顶点的度都为偶数，存在一条欧拉回路，使得能够一次性遍历所有的边，符合Hierholzer算法的应用条件，我们选取顶点<code>1</code>为邮局的位置（起点）：</p><ol type="1"><li>对<code>1</code>的所有关联边进行遍历，其相邻点有：<code>2,3,4,5</code></li><li>选取<code>1</code>的相邻点<code>2</code>，<code>1-2</code>没有被访问过，访问<code>1-2</code></li><li>对<code>2</code>的所有关联边进行遍历，其相邻点有：<code>1,3</code></li><li>选取<code>2</code>的相邻点<code>1</code>，<code>2-1</code>已经被访问过，不再访问</li><li>选取<code>2</code>的相邻点<code>3</code>，<code>2-3</code>没有被访问过，访问<code>2-3</code></li><li>对<code>3</code>的所有关联边进行遍历，其相邻点都有：<code>1,2</code></li><li>选取<code>3</code>的相邻点<code>1</code>，<code>3-1</code>没有被访问过，访问<code>3-1</code></li><li>对<code>1</code>的所有关联边进行遍历，其相邻点有：<code>2,3,4,5</code></li><li>选取<code>1</code>的相邻点<code>2,3</code>，<code>1-2</code>和<code>1-3</code>都被访问过，不再访问</li><li>选取<code>1</code>的相邻点<code>4</code>，<code>1-4</code>没有被访问过，访问<code>1-4</code></li><li>对<code>4</code>的所有关联边进行遍历，其相邻点有：<code>1,5</code></li><li>选取<code>4</code>的相邻点<code>1</code>，<code>4-1</code>已经被访问过，不再访问</li><li>选取</li></ol><p>代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">info</span><br>&#123;<br>    <span class="hljs-type">int</span> to,dis;<br>&#125;;<br>std::vector&lt;info&gt; g[<span class="hljs-number">55</span>];<br>std::stack&lt;<span class="hljs-type">int</span>&gt; path;<br><span class="hljs-type">bool</span> vis[<span class="hljs-number">55</span>][<span class="hljs-number">55</span>]；<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Hierholzer</span><span class="hljs-params">(<span class="hljs-type">int</span> now)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(info i:g[now])<br>        <span class="hljs-keyword">if</span>(!vis[now][i.to])&#123;<br>            vis[now][i.to]=<span class="hljs-literal">true</span>;<br>            vis[i.to][now]=<span class="hljs-literal">true</span>;<br>            <span class="hljs-built_in">Hierholzer</span>(i.to);<br>        &#125;<br>    path.<span class="hljs-built_in">push</span>(now);<br>&#125;<br></code></pre></td></tr></table></figure><p>在上述代码中：</p><ul><li><code>info</code>为顶点的相关信息，<code>to</code>表示与当前顶点相连的下一个顶点的编号，<code>dis</code>表示当前节点到下一个结点的距离（其实也可以用<code>std::pair</code>来实现，不过我觉得定义一个新的结构体比较直观一点）</li><li><code>g[now]</code>为<code>std::vector</code>类型的<strong>邻接表</strong>，用于存储当前节点相连的下一个节点的<code>info</code></li><li><code>path</code>为用于存储最终的欧拉回路路径</li><li><code>vis[now][i.to]</code>用于判断当前<strong>边</strong>是否有被访问过，由于是无向图，所以往返路径都要标记为已访问</li></ul><p>由于欧拉回路能一次性遍历所有边，故当访问到当前顶点没有关联边的时候，其在路径中的位置也就随之确定。</p>]]></content>
    
    
    
    <tags>
      
      <tag>算法</tag>
      
      <tag>数据结构</tag>
      
      <tag>图论</tag>
      
      <tag>欧拉图</tag>
      
      <tag>欧拉回路</tag>
      
      <tag>最短路径</tag>
      
      <tag>Hierholzer</tag>
      
      <tag>Floyd</tag>
      
      <tag>Dijkstra</tag>
      
      <tag>DFS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>基于LNMP框架自建树莓派Web服务器</title>
    <link href="/%E5%9F%BA%E4%BA%8ELNMP%E6%A1%86%E6%9E%B6%E8%87%AA%E5%BB%BA%E6%A0%91%E8%8E%93%E6%B4%BEWeb%E6%9C%8D%E5%8A%A1%E5%99%A8/"/>
    <url>/%E5%9F%BA%E4%BA%8ELNMP%E6%A1%86%E6%9E%B6%E8%87%AA%E5%BB%BA%E6%A0%91%E8%8E%93%E6%B4%BEWeb%E6%9C%8D%E5%8A%A1%E5%99%A8/</url>
    
    <content type="html"><![CDATA[<p>前段时间正在逛赞噢校园集市的时候，偶然发现一个帖子出二手<strong>RaspberryPi4B</strong>，价格也非常适中（指五折），欣喜之下收了这块九成新的板子。我在很早之前是有一块RaspberryPi3B的，但是由于当时专业知识不够，不知道能用树莓派干些什么，所以这块3B也一直放在柜子里吃灰（泪）。不过大一学习了Linux系统的相关知识之后，查阅资料发现有不少人用树莓派来<u>搭建个人服务器</u>，再加上4B性能方面相较于3B有了很大的提升，于是我尝试着在宿舍利用树莓派搭建一个私人小型服务器来供学习使用。</p><span id="more"></span><p>最开始我想利用树莓派搭建一个NAS私有云文件管理系统，不过想了想在宿舍其实用处不大，再加上树莓派的处理速度有限，很难有比较好的使用体验。后来我想到了目前比较火热的<strong>用于个人使用的ECS云服务器</strong>。树莓派完全能够胜任小型个人网站的部署，于是我着手利用树莓派搭建一个Web服务器。</p><h2 id="树莓派">1. 树莓派</h2><p>截至目前（2023.11.17），树莓派已经发行到Raspberry Pi5（应该还在预售？），各大电商厂家都能够买到，<strong>4B</strong>的均价在<strong>￥400</strong>左右，<strong>3B</strong>的均价在<strong>￥300</strong>左右，此外树莓派还有其他如ZeroW等型号。本文采用的是<strong>Raspberry Pi 4B</strong>。</p><blockquote><p>树莓派是RaspberryPi基金会用于教育使用的卡片式电脑，最初售价为$30左右，但是由于全球芯片危机，芯片半导体供应紧张限制了电子设备产业发展，在全球缺芯问题的影响下于2021年首次涨价，不过近几年树莓派的价格趋于平缓。</p></blockquote><p>树莓派本身继承很多个人电脑的特点，将所有的元件集成到一块基于ARM的微型电脑主板上，具备所有PC的基本功能。目前开发者已经推出了适用于树莓派的Linux、Windows系统，只要你有一张烧录好的TF卡，就能随时开启属于你的个人电脑（不过是只有电路板的电脑罢了）！树莓派的功耗非常小，在满载的状态下测试一年电费才￥30左右，所以不用担心树莓派的功耗问题。</p><p>本文所搭建的服务器采用<strong><em>Ubuntu Server for RaspberryPi</em></strong>系统，所以在阅读本文的时候，最好有一点Linux操作系统的知识哦。</p><p>在正式开始搭建服务器，<strong>请准备好以下几件东西：</strong></p><ul><li><strong>Raspberry Pi</strong></li><li><strong>TF卡（内存够用就行）</strong></li><li><strong>读卡器</strong></li><li><strong>个人电脑</strong></li></ul><h2 id="ubuntu-server">2. Ubuntu Server</h2><h3 id="下载镜像文件">2. 1 下载镜像文件</h3><p>众所周知，Linux操作系统起源于Unix操作系统，有很多版本可以选择，而<strong>Ubuntu</strong>是目前受众面最广的版本。Ubuntu的开发者专门开发了适用于树莓派的<strong><em>UbuntuServer for Raspberry Pi</em></strong>系统，现在请前往 <ahref="https://ubuntu.com/download/raspberry-pi">Install Ubuntu on aRaspberry Pi</a> 下载操作系统镜像文件</p><figure><img src="/img/基于LNMP框架自建树莓派Web服务器/ubuntuserver.jpg"alt="找到并选择图中红框内的下载" /><figcaption aria-hidden="true">找到并选择图中红框内的下载</figcaption></figure><p>由于本文将在终端中进行而不会用到图形化界面，为了节约TF卡里少量的空间<u>请选择Server版本而不要下载Desktop版本</u>。下载结束之后，你应该会得到类似于<code>ubuntu-22.04.3-preinstalled-server-arm64+raspi.img.xz</code>文件名的镜像文件。</p><h3 id="烧录tf卡">2. 2 烧录TF卡</h3><blockquote><p>如果你熟练掌握如何将一个操作系统烧录到一张TF卡上，请跳过这一步。</p><p>如果你不太清楚如何烧录且你的TF不是新的，<strong><u>请提前对TF卡上重要文件进行备份</u></strong>以免重要数据丢失。</p></blockquote><p>下载<strong><ahref="https://www.diskgenius.cn/download.php">DiskGenius</a>（磁盘格式化工具）</strong>，用读卡器将TF卡插入你的个人电脑，依次对TF卡进行【删除分区】-&gt;【新建分区】-&gt;【保存更改】-&gt;【格式化】。</p><p>下载<strong><ahref="https://rufus.ie/zh/">Rufus</a>（镜像烧录工具）</strong>，【设备】选择TF卡（名字应该是NO_LABEL），【镜像文件】选择刚刚下载的那个镜像文件，其余默认，点击【确认】等待烧录完成。</p><h3 id="网络设置">2. 3 网络设置</h3><h4 id="有显示屏">2. 3. 1 有显示屏</h4><p>如果你有一块显示屏，那么连接到树莓派之后，将TF卡插入树莓派中，屏幕上应该会显示让你输入密码，默认初始密码为<code>ubuntu</code>，输入完之后会让你更改密码。进入之后，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo -i<br>vim /etc/netplan/50-cloud-init.yaml<br></code></pre></td></tr></table></figure><p>进入配置文件后，点击键盘上的<code>i</code>键，再将配置文件修改成以下内容：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># This file is generated from information provided by the datasource.  Changes</span><br><span class="hljs-comment"># to it will not persist across an instance reboot.  To disable cloud-init&#x27;s</span><br><span class="hljs-comment"># network configuration capabilities, write a file</span><br><span class="hljs-comment"># /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg with the following:</span><br><span class="hljs-comment"># network: &#123;config: disabled&#125;</span><br><span class="hljs-attr">network:</span><br>    <span class="hljs-attr">ethernets:</span><br>    <span class="hljs-attr">eth0:</span><br>    <span class="hljs-attr">dhcp4:</span> <span class="hljs-literal">true</span><br>    <span class="hljs-attr">optional:</span> <span class="hljs-literal">true</span><br>    <span class="hljs-attr">version:</span> <span class="hljs-number">2</span><br>    <span class="hljs-attr">wifis:</span><br>    <span class="hljs-attr">wlan0:</span><br>    <span class="hljs-attr">access-points:</span><br>    <span class="hljs-attr">&quot;your_wifi_name&quot;:</span><br>    <span class="hljs-attr">password:</span> <span class="hljs-string">your_wifi_password</span><br>    <span class="hljs-attr">dhcp4:</span> <span class="hljs-literal">true</span><br>    <span class="hljs-attr">optional:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure><p><strong>注意！<u>请严格检查缩进</u></strong>，有的是2格缩进，有的是4格缩进，请以自己文件里面的缩进为准。其中<code>your_wifi_name</code>和<code>your_wifi_password</code>分别用你对应的Wifi替换。修改完成之后，点击<code>Esc</code>键，再输入<code>:wq</code>即可退出编辑。</p><p>回到终端后输入以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> wpa_supplicant<br>systemctl start wpa_supplicant<br>netplan generate<br>systemctl daemon-reload<br>netplan apply<br></code></pre></td></tr></table></figure><h4 id="无显示屏">2. 3. 2 无显示屏</h4><p>如果你不幸没有一块显示屏，那么保持你的TF卡插在你的个人电脑上，<strong>不要把TF卡插到树莓派上</strong>。上一步完成之后，可能会出现<strong>找不到磁盘</strong>的情况，应该是因为烧录的文件系统与Windows的文件系统不匹配，Windows无法识别。如果你的磁盘发生了丢失，但是电脑显示插入了这个设备，那么接下来的操作<u>请在DiskGenius进行</u>（DiskGenius中能识别烧录的文件系统）。</p><p>在TF卡中的<code>system-boot</code>下找到<code>network-config</code>，选择用【记事本】打开，按照【<strong>2.3. 1有显示屏</strong>】的步骤进行修改即可。保存退出后，就可以把TF卡插到树莓派里面了。</p><h3 id="ssh连接">2. 4 SSH连接</h3><p>在浏览器登录到路由器的内网管理系统中（TP-Link的应该是192.168.1.1，其他厂家请查看路由器后的地址），在管理页面找到一个用户名为【ubuntu】的用户，记录<strong>IP地址</strong>。SSH连接工具很多，比如Putty，Xshell等等。本文为了演示方便使用<strong>Windows终端自带的SSH</strong>连接。</p><p>打开Windows终端，输入以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ssh -v ubuntu@Your_raspi_IP<br></code></pre></td></tr></table></figure><p>其中<code>Your_raspi_IP</code>填写刚刚查到的IP地址。连接过程中会让你输入密码，默认密码为<code>ubuntu</code>，如果修改过了就填写修改过的密码。连接成功后，你将会看到下面的画面：</p><figure><img src="/img/基于LNMP框架自建树莓派Web服务器/ssh.png"alt="如果出现这个画面表示连接成功" /><figcaption aria-hidden="true">如果出现这个画面表示连接成功</figcaption></figure><h3 id="更换系统软件源">2. 5 更换系统软件源</h3><p>如果看到这一步都没有问题的话，恭喜你已经有了一台基本可以操作的服务器了，只要是处于同一互联网下可以随时进行操作。更换系统软件源是为了让系统的软件更新速度加快（因为默认软件源在国外，所以时不时会连不上），本文以清华镜像源为例。</p><p>进入<ahref="https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu-ports/">清华镜像源</a>，选择Ubuntu系统（根据你烧录的版本进行选择），其他默认就好，下面代码框中的内容就是待会儿需要添加的内容。</p><p>然后到SSH终端中，输入以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo -i<br>vim /etc/apt/sources.list<br></code></pre></td></tr></table></figure><p>把刚刚清华镜像源代码框里面的内容复制到里面去（如果你对Vim的操作还不是太熟悉的话，建议去学习一些简单的操作）：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释</span><br><span class="hljs-string">deb</span> <span class="hljs-string">https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/</span> <span class="hljs-string">jammy</span> <span class="hljs-string">main</span> <span class="hljs-string">restricted</span> <span class="hljs-string">universe</span> <span class="hljs-string">multiverse</span><br><span class="hljs-comment"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/ jammy main restricted universe multiverse</span><br><span class="hljs-string">deb</span> <span class="hljs-string">https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/</span> <span class="hljs-string">jammy-updates</span> <span class="hljs-string">main</span> <span class="hljs-string">restricted</span> <span class="hljs-string">universe</span> <span class="hljs-string">multiverse</span><br><span class="hljs-comment"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/ jammy-updates main restricted universe multiverse</span><br><span class="hljs-string">deb</span> <span class="hljs-string">https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/</span> <span class="hljs-string">jammy-backports</span> <span class="hljs-string">main</span> <span class="hljs-string">restricted</span> <span class="hljs-string">universe</span> <span class="hljs-string">multiverse</span><br><span class="hljs-comment"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/ jammy-backports main restricted universe multiverse</span><br><br><span class="hljs-string">deb</span> <span class="hljs-string">http://ports.ubuntu.com/ubuntu-ports/</span> <span class="hljs-string">jammy-security</span> <span class="hljs-string">main</span> <span class="hljs-string">restricted</span> <span class="hljs-string">universe</span> <span class="hljs-string">multiverse</span><br><span class="hljs-comment"># deb-src http://ports.ubuntu.com/ubuntu-ports/ jammy-security main restricted universe multiverse</span><br><br><span class="hljs-comment"># 预发布软件源，不建议启用</span><br><span class="hljs-comment"># deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/ jammy-proposed main restricted universe multiverse</span><br><span class="hljs-comment"># # deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports/ jammy-proposed main restricted universe multiverse</span><br></code></pre></td></tr></table></figure><p>更换完之后保存退出。在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo apt update &amp;&amp; apt -y upgrade<br></code></pre></td></tr></table></figure><p>使你的软件源配置更新并为你的系统获取最新的软件包。</p><h3 id="设置静态局域网ip">2. 6 设置静态局域网IP*</h3><p>由于UbuntuServer默认打开<strong>DHCP</strong>，所以可能会出现每次重启之后局域网IP地址会发生改变的情况，导致SSH连接不上服务器等问题。因此在服务器运维中设置一个<strong>静态局域网IP</strong>是非常有必要的。</p><p>在终端中输入如下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo vim /etc/netplan/50-cloud-init.yaml<br></code></pre></td></tr></table></figure><p>可能<code>netplan</code>文件夹下的文件名不同，请以自己文件夹下的那个文件名为准。</p><p>将配置文件中的内容修改为以下内容：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># This file is generated from information provided by the datasource.  Changes</span><br><span class="hljs-comment"># to it will not persist across an instance reboot.  To disable cloud-init&#x27;s</span><br><span class="hljs-comment"># network configuration capabilities, write a file</span><br><span class="hljs-comment"># /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg with the following:</span><br><span class="hljs-comment"># network: &#123;config: disabled&#125;</span><br><span class="hljs-attr">network:</span><br>    <span class="hljs-attr">ethernets:</span><br>        <span class="hljs-attr">eth0:</span><br>            <span class="hljs-attr">dhcp4:</span> <span class="hljs-literal">true</span><br>            <span class="hljs-attr">optional:</span> <span class="hljs-literal">true</span><br>    <span class="hljs-attr">version:</span> <span class="hljs-number">2</span><br>    <span class="hljs-attr">wifis:</span><br>        <span class="hljs-attr">wlan0:</span><br>            <span class="hljs-attr">access-points:</span><br>                <span class="hljs-attr">&quot;Your_wifi_name&quot;:</span><br>                    <span class="hljs-attr">password:</span> <span class="hljs-string">Your_wifi_password</span><br>            <span class="hljs-attr">dhcp4:</span> <span class="hljs-literal">false</span><br>            <span class="hljs-attr">addresses:</span><br>                <span class="hljs-bullet">-</span> <span class="hljs-number">192.168</span><span class="hljs-number">.1</span><span class="hljs-number">.111</span><span class="hljs-string">/24</span> <span class="hljs-comment"># Your_Static_IP/Mask</span><br>            <span class="hljs-attr">routes:</span><br>                <span class="hljs-bullet">-</span> <span class="hljs-attr">to:</span> <span class="hljs-string">default</span><br>                  <span class="hljs-attr">via:</span> <span class="hljs-number">192.168</span><span class="hljs-number">.1</span><span class="hljs-number">.1</span> <span class="hljs-comment"># Your_gateway</span><br>            <span class="hljs-attr">nameservers:</span><br>                <span class="hljs-attr">addresses:</span><br>                    <span class="hljs-bullet">-</span> <span class="hljs-number">223.5</span><span class="hljs-number">.5</span><span class="hljs-number">.5</span> <span class="hljs-comment">#DNS</span><br>                    <span class="hljs-bullet">-</span> <span class="hljs-number">8.8</span><span class="hljs-number">.8</span><span class="hljs-number">.8</span><br>            <span class="hljs-attr">optional:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure><p>保存退出之后，在再次终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl <span class="hljs-built_in">enable</span> wpa_supplicant<br>systemctl start wpa_supplicant<br>netplan generate<br>systemctl daemon-reload<br>netplan apply<br></code></pre></td></tr></table></figure><p>重启之后静态局域网IP地址就应该生效了。（不过好像会自动断连，应该是修改成功了）</p><h2 id="lnmp">3. LNMP</h2><p>至此，一个基本的UbuntuServer系统就搭建好了。在这个系统基础上，我们来搭建<strong>Web服务器</strong>。</p><h3 id="什么是lnmp">3. 1 什么是LNMP？</h3><p>LNMP，其实是<strong>Linux+Nginx+MySQL+PHP</strong>框架的简称，是当下最流行的Web服务器框架之一。</p><ul><li><p><strong>L</strong>指的是Linux操作系统，服务器所处的环境是处在Linux操作系统下。</p></li><li><p><strong>N</strong>指的是Nginx，是一个高性能的HTTP和反向代理web服务器，具有性能稳定、功能丰富、运维简单、处理静态文件速度快且消耗系统资源极少的特点，在服务器中用于发布静态页面和使用Fast-CGI调用PHP-FPM解析动态页面。</p></li><li><p><strong>M</strong>指的是MySQL，是目前最流行的关系型数据库管理系统之一。</p></li><li><p><strong>P</strong>指的是PHP，是在服务器端执行的超文本预处理器脚本语言，PHP-FPM将来自Nginx的动态PHP请求解析成HTML文件返回给Nginx。</p></li></ul><p>在LNMP流行之前，还有个LAMP框架，其中A指的是Apache，在对于PHP动态资源的处理上略有不同。但相比Apache，Nginx使用更少的资源，支持更多的并发连接，体现更高的效率。</p><p>整个服务器的<strong>作用原理</strong>其实很简单：</p><ul><li>用户在浏览器中请求HTTP请求。</li><li>Nginx接收到请求。若为静态请求则直接解析返回；若为动态请求，则调用Fast-CGI接口，发送给PHP-FPM模块解析。</li><li>若动态请求需要调用数据库，PHP-FPM则将调用数据库请求发送给MySQL数据库，读取需要的数据。</li><li>将解析结果通过Fast-CGI返回Nginx，Nginx再将解析结果发送到用户浏览器。</li><li>用户浏览器通过返回来的内容进行解析渲染呈现出来。</li></ul><h3 id="安装lnmp框架">3. 2 安装LNMP框架</h3><h4 id="安装nginx">3. 2. 1 安装Nginx</h4><p>打开SSH终端，先安装相关的依赖文件。在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo apt install -y gcc zlib1g-dev libpcre3 libpcre3-dev openssl libssl-dev<br></code></pre></td></tr></table></figure><p>然后输入以下命令安装Nginx：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo apt install -y nginx<br></code></pre></td></tr></table></figure><p>安装完成之后，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo service nginx start<br></code></pre></td></tr></table></figure><p>此时Nginx服务已经启动，在其他设备的浏览器地址栏中输入树莓派的IP地址。你将会看到以下页面：</p><figure><img src="/img/基于LNMP框架自建树莓派Web服务器/nginx.png"alt="如果出现这个画面表示开启成功" /><figcaption aria-hidden="true">如果出现这个画面表示开启成功</figcaption></figure><p>此时Nginx开启并正常运行。你可以在终端中输入如下命令来检查Nginx的运行状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo service nginx status<br></code></pre></td></tr></table></figure><p><imgsrc="/img/基于LNMP框架自建树莓派Web服务器/nginx-status.png" /></p><h4 id="安装mysql">3. 2. 2 安装MySQL</h4><p>打开SSH终端，输入以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo apt install -y mysql-server<br></code></pre></td></tr></table></figure><h4 id="安装php">3. 2. 3 安装PHP</h4><p>打开SSH终端，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo apt-get install -y php8.1-cli php8.1-dev php8.1-fpm php8.1-cgi php8.1-mysql php8.1-gd php8.1-curl php8.1-memcache<br></code></pre></td></tr></table></figure><p>版本号可以选择你想要的版本号。</p><h4 id="配置nginx连接php">3. 2. 4 配置Nginx连接PHP</h4><p>Nginx默认没有与PHP建立连接，为了Nginx能够调用PHP-FPM需要在配置文件中手动打开。在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo vim /etc/nginx/sites-enabled/default<br></code></pre></td></tr></table></figure><p>然后将配置文件找到如下部分进行修改：</p><figure><img src="/img/基于LNMP框架自建树莓派Web服务器/nginx-php.png"alt="添加内容或解开相关的注释，PHP版本选择你下载的版本" /><figcaptionaria-hidden="true">添加内容或解开相关的注释，PHP版本选择你下载的版本</figcaption></figure><p>保存退出，在终端中输入以下命令检查配置文件是否正确并重启Nginx使配置文件生效：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">nginx -t<br>sudo service nginx restart<br></code></pre></td></tr></table></figure><p>此时做一个PHP探针检验下配置文件是否生效，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> /var/www/html/phpinfo.php<br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&lt;?php phpinfo(); ?&gt;&quot;</span> &gt;&gt; /var/www/html/phpinfo.php<br></code></pre></td></tr></table></figure><p>然后在浏览器地址栏中输入<code>Your_raspi_IP/phpinfo.php</code>，你将会看到以下画面：</p><p><img src="/img/基于LNMP框架自建树莓派Web服务器/phpinfo.png" /></p><p>至此，LNMP框架基本搭建完成。</p><h2 id="内网穿透ssl证书安装">4. 内网穿透（SSL证书安装）</h2><p>恭喜，你现在有了一个可以在局域网内访问的服务器！不过如果你想把你的服务器放在公网中访问该怎么办呢？</p><p>第一种方法是到网络运营商处申请公网IP（听别人说要死缠烂打工人师傅才会给你==），然后通过公网IP进行访问。</p><p>第二种方式是<strong>内网穿透</strong>，即NAT穿透，实质是利用路由器上的NAT系统，将私有（保留）地址转化为合法IP地址的转换技术。通过代理服务器建立内网机器(Client)发起向外网服务器(Server)的连接。目前比较好用的内网穿透服务有Cpolar，Ngrok等等，可以自行搜索相关的教程进行配置。</p><p>这里简单说下关于SSL证书安装的方法。如果你想让你的自定义域名建立HTTPS的安全链接，建议在CDN服务商（如Cloudflare）处更改SSL加密模式，配置SSL源服务器证书。</p><p>我在网络上找了很多SSL证书安装教程，与Ubuntu Server for RaspberryPi系统的安装略有出入，下面我介绍本系统的安装方法。</p><h3 id="生成ssl证书">4. 1 生成SSL证书</h3><p>以Cloudflare为例，在你的域名站点处左侧找到【<strong>SSL/TLS</strong>】，在【概述】中将加密模式改为<strong>完全</strong>。</p><p>找到【源服务器】，在【源证书】处点击【创建证书】：</p><p><img src="/img/基于LNMP框架自建树莓派Web服务器/ssl.png" /></p><p>在主机名选择你自己服务器的域名：</p><p><imgsrc="/img/基于LNMP框架自建树莓派Web服务器/ssl_install.png" /></p><p>新建文本文档将里面的源证书和私钥保存下来备用：</p><p><img src="/img/基于LNMP框架自建树莓派Web服务器/pem_key.png" /></p><h3 id="安装ssl证书">4. 2 安装SSL证书</h3><p>打开SSH终端，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo vim /etc/nginx/cert/ssl.pem<br></code></pre></td></tr></table></figure><p>然后在配置文件中添加刚刚生成的源证书（Ctrl C + Ctrl V），保存退出</p><p>同理添加私钥：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo vim /etc/nginx/cert/ssl.key<br></code></pre></td></tr></table></figure><h3 id="配置nginx">4. 3 配置Nginx</h3><p>打开SSH终端，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo vim /etc/nginx/sites-enabled/default<br></code></pre></td></tr></table></figure><p>解除图示位置的注释，开启443端口：</p><p><img src="/img/基于LNMP框架自建树莓派Web服务器/443.png" /></p><p>在配置文件中找到添加或修改如下内容：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-string">root</span> <span class="hljs-string">/var/www/blog;</span><br><br><span class="hljs-comment"># Add index.php to the list if you are using PHP</span><br><span class="hljs-string">index</span> <span class="hljs-string">index.html</span> <span class="hljs-string">index.htm</span> <span class="hljs-string">index.nginx-debian.html</span> <span class="hljs-string">index.php;</span><br><br><span class="hljs-string">ssl_certificate</span> <span class="hljs-string">/etc/nginx/cert/ssl.pem;</span><br><span class="hljs-string">ssl_certificate_key</span> <span class="hljs-string">/etc/nginx/cert/ssl.key;</span><br><span class="hljs-string">server_name</span> <span class="hljs-string">Your_site_domain;</span><br><br><span class="hljs-string">location</span> <span class="hljs-string">/</span> &#123;<br>        <span class="hljs-comment"># First attempt to serve request as file, then</span><br>        <span class="hljs-comment"># as directory, then fall back to displaying a 404.</span><br>        <span class="hljs-string">try_files</span> <span class="hljs-string">$uri</span> <span class="hljs-string">$uri/</span> <span class="hljs-string">=404;</span><br>&#125;<br></code></pre></td></tr></table></figure><p>将<code>Your_site_domain</code>修改成你的域名。保存退出后在终端中输入以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">nginx -t<br>sudo service nginx restart<br></code></pre></td></tr></table></figure><p>再次访问你的域名，就能看到证书配置成功了！</p><h2 id="总结">5. 总结</h2><p>到这里Web服务器的框架搭建就基本完成了。如果你一路顺风顺水地看到这里的话，博主还是蛮佩服的（毕竟我花了将近一周的时间查阅资料才基本搞懂这里面的原理ww），同时也希望给在此时正在搭建Web服务器并遇到困难的朋友提供一点点帮助，希望看到我的博客能解决你和我同时产生的问题。</p><p>搭建的服务器能干嘛呢？这就要看你自由发挥了。有的人用服务器部署个人博客（如WordPress，Hexo等等），有的人搭建游戏服务器（<del>三国杀</del>，Minecraft等等），有的人用做邮件代理服务器，有的人用做NAS文件管理系统，还有的人创建独立运行环境跑实验项目……总之服务器的功能还有很多啦。不过树莓派功率还是比较小，不适合跑大型项目，仅供学习交流。</p><p>感谢你的阅读！</p>]]></content>
    
    
    
    <tags>
      
      <tag>服务器</tag>
      
      <tag>树莓派</tag>
      
      <tag>Raspberry Pi</tag>
      
      <tag>LNMP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hexo静态博客搭建（基础篇）</title>
    <link href="/Hexo%E9%9D%99%E6%80%81%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA%EF%BC%88%E5%9F%BA%E7%A1%80%E7%AF%87%EF%BC%89/"/>
    <url>/Hexo%E9%9D%99%E6%80%81%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA%EF%BC%88%E5%9F%BA%E7%A1%80%E7%AF%87%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<p>如果你某一天有了一个<strong>搭建个人博客</strong>的想法，但是因为<u>资金不足</u>、<u>技术栈太浅</u>等原因导致难以从零开始搭建自己的网站，那么用<strong>Hexo</strong>搭建一个静态博客就是一个不错的选择！本站是也是一个基于Hexo搭建的个人博客，将我的搭建过程分享出来，供感兴趣的朋友参考。</p><p><span id="more"></span></p><h2 id="什么是hexo">1. 什么是Hexo？</h2><p>在搭建Hexo静态博客之前，先了解下什么是Hexo以及需要准备的前期工作。</p><p>Hexo是一个快速、简洁且高效的博客框架（<em>Official</em>），将个人博客部署到Github上，通过Markdown格式展现博客内容，相较于其他有后端的如Wordpress等，上手更快、更适合新手搭建博客。（由于只涉及到一些前端的内容，所以对后端不太熟悉也完全没有问题！）</p><p>在搭建博客之前，<strong>你需要准备好以下几个内容：</strong></p><ul><li>一台搭载Windows/Linux系统的电脑（如果你是Mac系统，建议查看<ahref="https://hexo.io/zh-cn/docs/">官方文档</a>，在这里就不进行赘述了）</li><li>能科学上网的工具</li><li>注册一个Github账号</li><li>熟悉Markdown的使用方法（不会其实也没问题，学习之后能更快上手博客文章的写作）</li></ul><p>准备好上面的东西之后，那让我们开始吧。</p><h2 id="安装hexo">2. 安装Hexo</h2><h3 id="准备工作">2. 1 准备工作</h3><p>在正式安装Hexo之前，<strong>确保你的电脑上面有安装有</strong>：</p><ul><li><a href="https://nodejs.org/">Nodejs</a></li><li><a href="https://git-scm.com/">Git</a></li></ul><p>Nodejs为JavaScript提供运行环境，Git用于将你的博客部署到Github上。</p><p>Windows用户可直接<strong>在官网上下载安装</strong>，Linux用户可以通过<strong>终端</strong>获取：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo apt-get install nodejs<br>sudo apt-get install npm<br>sudo apt-get install git<br></code></pre></td></tr></table></figure><p>同时确保你的版本为最新版本，避免出现版本不兼容的问题。</p><h3 id="安装hexo-1">2. 2 安装Hexo</h3><p>打开终端，输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install -g hexo-cli<br></code></pre></td></tr></table></figure><p>这个命令下载了Hexo部署博客的相关依赖，它提供了基本的部署生成工具，现在你可以使用这个工具来生成你的博客了。</p><p>此时你要创建一个<u>文件夹</u>用于在<strong>本地</strong>存储你的个人博客，在你想安装博客的目录下输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo init XX<br></code></pre></td></tr></table></figure><p>上面<code>XX</code>的部分填写任何名字均可（就是你能找到你部署博客的名字），然后在这个目录下就会生成一个新的名为<code>XX</code>的文件夹，进入该文件夹，输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install<br></code></pre></td></tr></table></figure><p>安装依赖文件。至此，一个简单的博客就搭建完成了。</p><p>搭建好博客之后，该怎么查看在网页上的效果呢？Hexo官方给了几个很简单的命令，帮助你更快上手Hexo博客搭建部署：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean <span class="hljs-comment"># 清理缓存和静态文件</span><br>hexo new [layout] &lt;title&gt; <span class="hljs-comment"># 生成新的文章</span><br>hexo generate <span class="hljs-comment"># 简记为 hexo g, 生成博客的静态文件</span><br>hexo server <span class="hljs-comment"># 简记为 hexo s, 启动本地服务器（查看部署到网页的效果）</span><br>hexo deploy <span class="hljs-comment"># 简记为 hexo d, 部署到网页</span><br></code></pre></td></tr></table></figure><p>更多命令请参考<ahref="https://hexo.io/zh-cn/docs/">官方文档</a>。现在，在这个文件夹打开的终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo g<br>hexo s<br></code></pre></td></tr></table></figure><p>等待一会儿直到你看到类似如下的图片：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/hexo初始化.png"alt="这是你看到的内容" /><figcaption aria-hidden="true">这是你看到的内容</figcaption></figure><p>然后在浏览器的搜索框中输入<code>localhost:4000</code>，就可以看到你部署的博客在网页中的样子了。如果你想退出预览，那么在刚才的终端页面中按下键盘上的<strong><em>Ctrl+C</em></strong> 即可退出本地预览。</p><h2 id="部署到github">3. 部署到Github</h2><h3 id="遇到的问题">3. 1 遇到的问题</h3><p>在退出本地预览之后，再次访问<code>localhost:4000</code>，发现居然无法访问了。可恶，搭建好的博客关掉本地服务器之后就不能再看了，有没有什么办法能让我通过网页直接浏览呢？答案是<strong>把网页部署到Github上</strong>。</p><p>Hexo生成静态博客的实质是生成了<strong>静态网页文件</strong>，然后通过访问这些网页文件达到浏览博客的效果。Github作为全球最大的代码托管平台，将本地的文件部署Github上，再通过Github提供的网址访问，就能直接在网页上浏览你的博客了。当然你也可以选择购买一台云服务器直接部署到你的云服务器上，不过博主手头比较紧辣，所以还是选择Free的Github吧。</p><h3 id="创建仓库">3. 2 创建仓库</h3><p>登陆到Github之后（如果登陆不上Github，建议检查是否缺少相应的条件），点击右上角的+ 号，再点击【<strong>New repository</strong>】：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/hexo%20repo1.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>进去之后，将这个仓库的名字改为 <code>XX.github.io</code>，其中<code>XX</code>为你的用户名（记住，一定要为你的<strong>用户名</strong>不是昵称！）：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/hexo%20repo2.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>然后其他选项保持默认即可（注意下方是否选择了【<strong>Public</strong>】选项以免不能通过网址访问）</p><h3 id="生成本地ssh">3. 3 生成本地SSH</h3><p>SSH是一种<strong>网络协议</strong>，让我们能够安全地登陆到Github上，有效防止远程管理过程中的信息泄露问题。其具体的实现过程如下：</p><ul><li>远程主机收到来自你电脑的登陆请求，将主机存储的SSH公钥与你电脑发送的<strong>公钥</strong>进行比对。</li><li>若公钥一致，主机将用你电脑发送的公钥加密“质询”(Challenge)并将加密内容返回你的电脑。</li><li>你的电脑用本地一并生成的<strong>私钥</strong>对”质询“进行解密并把解密内容发送给远程主机。</li><li>若解密内容一致，则允许登陆。</li></ul><p>让我们我们先在本地生成<strong>SSH秘钥</strong>。在你博客文件夹下打开终端，在终端中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git config --global user.name <span class="hljs-string">&quot;username&quot;</span><br>git config --global user.email <span class="hljs-string">&quot;useremail&quot;</span><br></code></pre></td></tr></table></figure><p>其中<code>username</code>换成你Github的用户名，<code>useremail</code>换成你注册Github所用的邮箱。可以用以下两条命令检查是否填写正确：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git config user.name<br>git config user.email<br></code></pre></td></tr></table></figure><p>接下来创建SSH秘钥，输入以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ssh-keygen -t rsa -C <span class="hljs-string">&quot;useremail&quot;</span><br></code></pre></td></tr></table></figure><p>一直回车，直到它告诉你已经生成了SSH密钥秘钥，打开它所给的文件夹：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/ssh1.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>其中，<code>id_rsa</code>是你这台电脑的私人秘钥，<code>id_rsa.pub</code>是公共秘钥，我们要用的就是<code>id_rsa.pub</code>。选择用记事本的形式打开<code>id_rsa.pub</code>，其中的内容就是我们要的公钥。</p><h3 id="github创建ssh-key">3. 4 Github创建SSH Key</h3><p>点击Github的头像，找到下方的【<strong>Settings</strong>】，在【<strong>Settings</strong>】中找到【<strong>SSHand GPG key</strong>】，也可以直接点击<ahref="https://github.com/settings/keys">链接</a>，然后点击【<strong>NewSSH key</strong>】</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/ssh2.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p><code>Title</code>随便取，<code>Key type</code>保持默认<code>Authentication key</code>，然后在<code>Key</code>中填入你上面生成的公共秘钥<code>id_rsa.pub</code>中的内容：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/ssh3.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>输入完成之后点下【<strong>Add SSH key</strong>】。</p><p>回到之前的终端，在终端中输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ssh -T git@github.com<br></code></pre></td></tr></table></figure><p>如出现如下的界面则创建SSH密钥成功：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/ssh4.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><blockquote><p>最新产生的问题：如果你在校园网环境下可能会失败，因为部分校园网会屏蔽SSH端口（比如我们学校的校园网会时常抽风）</p><p>解决方案：换成手机流量就可以了</p></blockquote><h3 id="配置_config.yml">3. 5 配置<code>_config.yml</code></h3><blockquote><p>2023.10.22更新：由于博主校园网持续屏蔽SSH端口，故放弃SSH的方法，改用HTTPS进行链接。</p></blockquote><p>在你的博客文件夹主目录下的<code>_config.yml</code>是Hexo的配置文件，用记事本打开后可以根据注释按照你的情况对其进行修改。现在我们需要配置最下面的<code>deploy</code>选项，根据你的情况按照下面的格式进行填写：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs markdown">deploy:<br>  type: git<br>  repo: https://github.com/usergithubname/usergithubname.github.io.git<br>  branch: master<br></code></pre></td></tr></table></figure><p>其中<code>repo</code>选择Github仓库中的HTTPS中的链接，即：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/config1.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>至此，部署到Github上的准备工作已经完成。</p><h3 id="部署到github-1">3. 6 部署到Github</h3><p>在部署之前，你需要下载一个部署插件才能使用<code>hexo d</code>，在终端中输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install hexo-deployer-git --save<br></code></pre></td></tr></table></figure><p>等待完成后，输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean<br>hexo g<br>hexo d<br></code></pre></td></tr></table></figure><p>当你看到如下的界面时，你的部署就大功告成！</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/config2.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>此时你的博客已经部署到Github的仓库中了，你可以在浏览器中输入<code>https://usergithubname.github.io/</code>来访问你的博客啦。这里<code>usergithubname</code>还是填你Github的用户名。</p><h2 id="设置个人域名">4. 设置个人域名</h2><h3 id="一点准备">4. 1 一点准备</h3><p>在部署中就会遇到一个问题，那就是如果关掉科学上网的工具，那网站就很难再访问了。但是如果我们想让没有科学上网工具的人也能浏览我们的博客应该怎么办呢？答案是<strong>设置个人域名</strong>。</p><p>我们在浏览器中输入<code>https://usergithubname.github.io/</code>打开我们的博客的时候，其中经历了三个步骤：</p><ul><li>浏览器根据域名向DNS服务器发送请求</li><li>DNS将域名解析到对应的IP地址</li><li>根据Http请求将网络路由接到对应IP地址的服务器</li></ul><p>本质上我们访问网站其实是访问网站的<strong>IP地址</strong>，域名只是起到一个“别名”的作用，域名和IP地址是<strong>键值对</strong>的关系。不过其实IP地址是一直在变化的，所以域名还能提供一个相对固定的访问地址。你的<code>https://usergithubname.github.io/</code>其实是<code>github.io</code>的二级域名，由于Github的服务器在国外所以访问速度会慢很多（<del>本着网线接的越长访问速度越慢的原理</del>）。那我们的域名是怎样加快访问速度的呢？</p><p>这里我们就要谈到<strong>CDN（内容分布网络）</strong>，它加快网络访问速度的原理其实很简单：由于我们的博客是静态资源，如果我们的博客在附近被访问过一次，那么本地的DNS服务器对应IP中就会有博客的缓存，当其他临近用户再次访问这个网站时，就能直接从最近的DNS服务器的缓存中访问了，大大提升了访问速度。</p><h3 id="域名申请">4. 2 域名申请</h3><p>免费域名申请有很多，比较流行的有<em>eu.org</em>，详情可以参考其他的博客（它是一个二级域名再加上我的还没有审批下来所以有点介意啦）</p><p>付费顶级域名在国内外的互联网大厂都能买到，我的域名 <em>.top</em>在阿里云购买的，首年￥9，次年￥29，对于学生党还是很友好了。</p><p>申请的具体过程就不赘述了，在申请的时候会有对应的提示，可以按照提示进行申请。</p><h3 id="域名设置">4. 3 域名设置</h3><p>首先打开终端，在终端中输入命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ping usergithubname.github.io<br></code></pre></td></tr></table></figure><p>在这个位置会显示你的博客IP地址：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/domain1.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>然后在你DNS解析商处打开你的域名管理，在DNS解析处添加两条记录：<strong>A</strong>和<strong>CNAME</strong></p><table><thead><tr class="header"><th style="text-align: center;">类型</th><th style="text-align: center;">名称</th><th style="text-align: center;">内容</th><th style="text-align: center;">TTL</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">A</td><td style="text-align: center;">@</td><td style="text-align: center;">[你刚才ping出来的IP地址]</td><td style="text-align: center;">自动/10min</td></tr><tr class="even"><td style="text-align: center;">CNAME</td><td style="text-align: center;">www</td><td style="text-align: center;">usergithubname.github.io</td><td style="text-align: center;">自动/10min</td></tr></tbody></table><p>这样确保你的DNS解析商知道你的域名是指向哪里（由于DNS缓存的原因要过上一小段时间才能更新，所以添加完还不能立马访问哦）</p><p>然后打开Github中你部署博客的仓库，在仓库上方找到<code>Settings</code>，打开后在左侧找到<code>Pages</code>，在里面找到<code>Custom domain</code>，并把你<strong>申请的域名</strong>添加到里面，记得勾选下面的<code>Enforce HTTPS</code>：</p><figure><img src="/img/Hexo静态博客搭建（基础篇）/domain2.png"alt="这时你会看到的界面" /><figcaption aria-hidden="true">这时你会看到的界面</figcaption></figure><p>然后在你的本地部署博客文件夹的主目录中找到<code>source</code>文件夹，在<code>source</code>文件夹下新建文本文档并命名为<code>CNAME.txt</code>，用记事本打开后输入<strong>你申请的域名</strong>，保存退出，然后删掉这个文件<code>CNAME.txt</code>的后缀<code>.txt</code>。</p><p>最后重新部署下你的博客：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean<br>hexo g -d<br></code></pre></td></tr></table></figure><p>上面这个命令可以在生成之后立即部署，然后你就可以用自己的域名进行访问啦。</p><blockquote><p>建议每次部署之前都进行上面的操作。</p></blockquote><h2 id="总结">5. 总结</h2><p>至此，一个可以通过互联网快速访问、可以任你创造的博客就创建好了。不过你要走的路还很长，包括网站的基础设置、网站主题的更换，评论系统、访问统计、SEO等等，这些都是为了让你的博客更好看、让别人能更好的浏览。说来我搭建博客也没多长时间，我要学的东西也有很多，之后我也会继续更新，努力把自己的博客建的更好。</p><p>感谢你的阅读！</p>]]></content>
    
    
    
    <tags>
      
      <tag>Hexo</tag>
      
      <tag>博客</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
